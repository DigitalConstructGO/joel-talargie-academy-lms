# Certificate storage

PDFs use the existing private S3-compatible provider. Keys are random and include certificate identity plus generation version but no student name or email. Students receive five-minute signed URLs only for owned `GENERATED` certificates. Administrators with download permission may access generated or revoked historical files.
