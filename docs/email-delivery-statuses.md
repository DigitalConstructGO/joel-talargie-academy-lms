# Email delivery statuses

Deliveries use `QUEUED`, `PROCESSING`, `SENT`, `RETRY_SCHEDULED`, `FAILED`, `CANCELLED`, or `SUPPRESSED`. Immutable attempts record processing, success, temporary failure, or permanent failure.
