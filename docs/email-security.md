# Email security

HTML values are escaped, plain text is normalized, recipients and headers reject injection, previews redact security URLs, credentials are environment-only, and secrets or bodies are not logged.
