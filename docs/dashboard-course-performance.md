# Dashboard course performance

Course performance is a grouped query, bounded to 20 results. It supports fixed sort metrics and uses the documented eligible-enrollment completion-rate denominator. Low-completion results require at least five eligible enrollments.
