# Dashboard API

All routes are under `/api/v1/admin/dashboard` and require `dashboard.read`. The overview and KPI routes accept a preset or custom bounded range. Trend routes use fixed registrations, enrollments, payments, revenue, completions, and certificate definitions. Preview limits are bounded; detailed investigation remains in Phase 11 report endpoints.
