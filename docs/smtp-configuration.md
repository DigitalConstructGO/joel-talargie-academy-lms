# SMTP configuration

SMTP credentials are server environment secrets. Pooling, timeouts, identities, worker limits, and retry delays are validated. With mail disabled, no SMTP connection is made and deliveries are safely suppressed.
