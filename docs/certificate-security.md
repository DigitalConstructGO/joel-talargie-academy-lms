# Certificate security

Ownership and dynamic permissions protect private operations. Public tokens have 256 bits of entropy. Snapshots are backend-derived, storage is private, URLs expire, template input is allowlisted, critical transitions use row locks, jobs are deduplicated, and public responses exclude secrets and internal IDs.
