# Dashboard comparison rules

The previous comparison range has the exact duration immediately preceding the selected range. Zero previous values produce a null percentage rather than Infinity or NaN.
