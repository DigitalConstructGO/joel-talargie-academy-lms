# Setting precedence

Deployment environment values are the upper security boundary. A database value cannot enable mail, Google authentication, certificate generation, report exports, upload size, or export row counts beyond the environment policy. Effective capability is environment permission AND database setting.
