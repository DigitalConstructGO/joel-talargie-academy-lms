# Certificate template configuration

Templates are versioned. Supported configuration keys are `academyName`, `title`, `primaryColor`, `accentColor`, and `footerText`. Values are bounded strings; colors are six-digit hex values. Executable markup, arbitrary paths, and unknown configuration keys are rejected.
