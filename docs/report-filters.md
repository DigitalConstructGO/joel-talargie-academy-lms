# Report filters

Common filters are `from`, `to`, status, course, Student, category, actor, safe search, grouping, page, page size, and sort direction. ISO timestamps are interpreted as instants, stored/query in UTC, and results are ordered by a timestamp plus ID.
