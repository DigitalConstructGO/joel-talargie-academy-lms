# Dashboard operational health

Operational health returns safe counts and internal action URLs for pending aged payments, failed certificates, failed emails, and failed report exports. It never includes workers, credentials, stack traces, storage keys, or message bodies.
