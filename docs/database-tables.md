# Database tables

Phase 11 adds `report_exports`, containing export request and lifecycle metadata. Generated rows are not stored; only private-file metadata, checksum, counts, attempts, safe failures, filters, ownership, and timestamps are retained.
