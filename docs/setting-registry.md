# Setting registry

Each definition records key, category, type, default, permission, editability, restart requirement, and description. Categories are academy, registration, payment, learning, certificates, notifications, and reports. The API has no arbitrary setting-creation endpoint.
