# Platform settings

Settings are backend-defined and typed. Updates require a reason, category permission, business validation, and an atomic audit event. Unknown keys, executable content, infrastructure secrets, privilege-escalating role defaults, and values beyond deployment hard limits are rejected.
