# Email template system

Templates are versioned by code, version, and locale, with one active version per code/locale. System templates are seeded idempotently; arbitrary public codes are forbidden.
