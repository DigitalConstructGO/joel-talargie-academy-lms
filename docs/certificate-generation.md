# Certificate generation

Eligible requests create `PENDING` identities. The worker renders a one-page PDF with PDFKit, embeds a QR generated by `qrcode`, validates the `%PDF-` signature, uploads privately, stores SHA-256 and size metadata, and changes status to `GENERATED`. Failures retry with bounded exponential delay and eventually become `FAILED`.
