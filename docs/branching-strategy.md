# Branching strategy

`main` represents releasable work. Phase development occurs on `develop`; focused branches should merge into it through reviewed pull requests. No remote is configured by Phase 1.
